# Exports (stub)

Planned exports:
- DOCX: use `docx` package to convert TipTap JSON → HTML → DOCX
- PDF: use `@react-pdf/renderer` or server-side HTML-to-PDF

MVP ships export placeholders. Next step is to implement:
- Select blocks in the editor
- Export selection or whole page
